import { StyleSheet, View, Text } from 'react-native'

const Button = () => {
  return (
    <View>
      <Text>Button</Text>
    </View>
  )
}

const styles = StyleSheet.create({})

export default Button
