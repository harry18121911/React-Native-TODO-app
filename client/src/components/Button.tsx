import { StyleSheet, } from 'react-native'
import { Box, Text } from './utils/theme'

const Button = () => {
  return (
    <Box bg='blu400' p='4' borderRadius='rounded-3xl'>
      <Text>Button</Text>
    </Box>
  )
}

const styles = StyleSheet.create({})

export default Button
